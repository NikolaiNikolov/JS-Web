extends ../layout

block content
    form.form.center-form(method='post')
        h2 Login
        .form-group
            label(for='username') Username
            input#username.input-field(name='username', type='text', value='pesho')
        .form-group
            label(for='password') Password
            input#password.input-field(name='password', type='password', value='123')

        .form-group
            input.form-btn.btn(type='submit', value='Login')
