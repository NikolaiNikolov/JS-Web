<section id="viewCatalog">
    <div class="posts">
        {{#if articles}}
            {{#each articles}}
                {{> article}}
            {{/each}}
        {{else}}
            <div>
                <h1 style="font-size: 35px;">No posts in database.</h1>
            </div>
        {{/if}}
    </div>
</section>