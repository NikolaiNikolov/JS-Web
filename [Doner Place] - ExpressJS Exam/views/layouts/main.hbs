<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Doner Place</title>
    <link href="/css/site.css" type="text/css" rel="stylesheet">
</head>
<body>
<div class="container">
    <header>
        <span class="logo">Doner  Place</span>
        {{#if currentUser}}
            <span class="greeting">Welcome, {{currentUser.username}}! |
            <form id="logoutForm" action="/logout" method="post">
                <a href="#" onclick="document.getElementById('logoutForm').submit()">[logout]</a>
            </form>
            </span>
        {{/if}}


        <ul class="menu">
            <li><a class="button" href="/">Home</a></li>
            <li><a class="button" href="/manage/orders">Order Status</a></li>
            {{#if currentUser}}
            {{else}}
                <li><a class="button" href="/login">Login</a></li>
                <li><a class="button" href="/register">Register</a></li>
            {{/if}}

        </ul>
    </header>
    <main>
        {{#if globalError}}
            <span style="color: red; font-size: 20px">{{globalError}}</span> {{/if}}
        {{{body}}}
    </main>
    <footer>Doner Place &copy; 2017</footer>
</div>
</body>
</html>